name: Daily Sync

on:
  schedule:
    # Runs twice daily at 8:07 AM and 8:07 PM UTC
    # Avoid the start-of-hour peak when scheduled runs can be delayed or dropped.
    - cron: '7 8,20 * * *'
  workflow_dispatch:
    # Allows manual trigger from GitHub Actions tab

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run sync
        env:
          DB_CONNECTION_STRING: ${{ secrets.DB_CONNECTION_STRING }}
          LAUNCHES_LINK: ${{ secrets.LAUNCHES_LINK }}
          ARTICLES_LINK: ${{ secrets.ARTICLES_LINK }}
        run: npm run sync
